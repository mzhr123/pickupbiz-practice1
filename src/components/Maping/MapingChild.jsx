import React from "react";

export const MapingChild =(props)=>{
    return(
        <div>
            <p>
                my company name is {props.company} and the car is {props.carName}
            </p>
        </div>
    );
};